#include <bits/stdc++.h>
using namespace std;

#define int long long
#define endl '\n'

int a[3], m;

signed main() {
    #ifdef lan_ngu
        freopen("test.inp", "r", stdin);
        freopen("test.out", "w", stdout);
    #endif

    int _nt;
    cin >> _nt;
    while (_nt--) {
        int x = 5;
        if (x = 0)
            cout << "hung ngu";
        else
            cout << "hung rat ngu";
        
    }
    return 0;
}